server:
  port: 8088

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sqs-demo
    username: postgres
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true